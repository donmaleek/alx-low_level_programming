#include<stdio.h>
#include<stdlib.h>
/**
 * divide - The function calculates the division of numbers
 * @a: The first number
 * @b: The second number
 * Return: The division of the two numbers
 */
int divide(int a, int b)
	{
	return (a / b);
	}
