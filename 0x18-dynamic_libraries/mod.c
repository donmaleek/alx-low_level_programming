#include<stdio.h>
#include<stdlib.h>
/**
 * mod - The function calculates the multiplication of numbers
 * @a: The first number
 * @b: The second number
 * Return: The modulus of the two numbers
 */
int mod(int a, int b)
	{
	return (a % b);
	}
