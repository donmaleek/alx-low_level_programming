#include <stdio.h>

/**
 * main - This program prints the given phrase
 * but the outcome is a piece of art,
 * Return: 0 if exited properly, otherwise non zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
